import React, { Component } from 'react'
import PropTypes from 'prop-types'

class Chat extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        return (
            <div>
                Chat per user
            </div>
        )
    }
}
export default Chat;